name: Self-Hosted Runner Example

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Build and Test
        run: |
           ls
          # For example:
          # npm install
          # npm run build
          # npm test
